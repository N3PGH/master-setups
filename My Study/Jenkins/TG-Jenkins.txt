maven project
{CMD}

[C:]git clone http:\
[C:]cd time-tracker

[time-tracker]mvn clean package

-------------------------------------
jenkins:

plugins

authorize project
________________________________________
scheduling & poll scm
post build action
build triggers after other projects are build

job-view list
-------------------
user management

create new user- by default user has all previlage

manage user role 
-----------------
